This Flutter app has an increment button and a reset button to control the counter.
It also shows a message when the counter goes above my personal threshold and tracks how many times reset is used.
setState() tells Flutter that some data has changed and the screen needs to update.
Without setState(), the variable may change internally, but the updated value will not appear on the screen.